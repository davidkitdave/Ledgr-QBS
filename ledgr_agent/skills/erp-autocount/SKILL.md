---
name: erp-autocount
description: AutoCount AP/AR import column layout for build_sheets.
metadata:
  author: ledgr-qbs
  version: "1.0"
---

# ERP export profile — AutoCount

Deterministic column map loaded by `build_sheets` via `ledgr_agent.internal.skill_profiles`.
Edit `assets/profile.yaml` only with golden-format test verification.
