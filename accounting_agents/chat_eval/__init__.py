"""Chat-lane eval entry package (agents-cli + ADK AgentEvaluator).

See ``accounting_agents/chat_eval/agent.py`` — the eval harness loads this
directory as a single-agent ADK module. ``App.name`` must match the directory
name (``chat_eval``) per agents-cli eval gotchas.
"""
