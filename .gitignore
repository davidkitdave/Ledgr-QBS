# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtualenv
.venv/
venv/

# Secrets / local env  (NEVER commit — contains Slack/API tokens)
.env
.env.*
!.env.example

# Tooling / local state
.omc/
.claude/settings.local.json
.claude/worktrees/
.adk/
**/.adk/
.DS_Store
*.log

# Agent output / scratch
invoice_processing/data/agent_output/
invoice_processing/data/alf_output/
# Generated eval results + QA screenshots (build output — never commit)
artifacts/grade_results/
# Generated agent traces (eval run output, may carry extracted doc data — never commit)
artifacts/traces/

# Local eval data (real invoices — never commit, see no-real-client-data rule)
tests/eval_invoices/
# Scratch: probes, QA docs/screenshots, real sample PDFs — never commit
scratch/
# Local playground profile may carry a real client name — never commit
playground_profile.json
# Subagent-driven-development scratch (task briefs, progress ledger) — local only
.superpowers/
# Machine-extracted golden manifest carries real vendor names + superseded by
# golden_v2_sample.json (synthetic) — never commit
tests/eval/datasets/golden_manifest.json
# Knowledge-graph tooling output
graphify-out/
.qa_tmp/
