# Superpowers (historical)

Implementation plans and specs for the **removed** `accounting_agents` /
`invoice_processing` era are under [`archive/`](archive/).

Live architecture: [docs/adr/README.md](../adr/README.md), [CONTEXT.md](../../CONTEXT.md).

The [`spikes/`](../spikes/) folder may contain one-off experiments — treat as non-authoritative unless referenced by a current ADR.
