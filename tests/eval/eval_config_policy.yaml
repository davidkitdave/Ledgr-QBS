metrics_to_run:
  - tax_validity_code

custom_metrics:
  - name: tax_validity_code
    custom_function: |
      from ledgr_agent.metrics.batch_result_metrics import tax_validity_code as _score

      def evaluate(instance):
          return _score(instance)
