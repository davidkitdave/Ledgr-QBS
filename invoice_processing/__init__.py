"""Invoice Processing -- deterministic pipeline engine.

The deterministic engine (``invoice_processing.pipeline`` and the
``classify`` / ``extract`` / ``export`` packages) is what the live Slack app
uses.
"""
