# ─── Gemini / AI Studio (NO Vertex) ─────────────────────────────────────────
# Use AI Studio key in both dev and prod (no Vertex quota needed).
GOOGLE_GENAI_USE_VERTEXAI=FALSE
GOOGLE_API_KEY=your-google-ai-studio-key-here

# ─── Model tiers (optional overrides) ─────────────────────────────────────────
# LEDGR_MODEL_LITE defaults to gemini-2.5-flash-lite  (coordinator + invoices)
# LEDGR_MODEL_STD  defaults to gemini-2.5-flash        (bank statements)
LEDGR_MODEL_LITE=gemini-2.5-flash-lite
LEDGR_MODEL_STD=gemini-2.5-flash

# ─── Google Cloud (Firestore for sessions + client profiles) ──────────────────
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json   # local only

# ─── Slack app credentials ────────────────────────────────────────────────────
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token          # socket-mode only (local dev)
SLACK_SIGNING_SECRET=your-signing-secret     # HTTP mode (Cloud Run)

# ─── Slack OAuth (Model B multi-workspace; leave blank for single-workspace) ──
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=

# ─── Cloud Run / server ───────────────────────────────────────────────────────
PORT=8080

# ─── Optional telemetry (GCS bucket for OTel traces; omit to disable) ─────────
# LOGS_BUCKET_NAME=gs://your-telemetry-bucket
